COPY YOUR DEMO SITE BOUNDARY HERE

Preferred shapefile name:
  demo_boundary.shp
  demo_boundary.shx
  demo_boundary.dbf
  demo_boundary.prj

Keep all shapefile components in this same folder.

The built-in demo also supports GeoJSON, GPKG, KML, KMZ and ZIP shapefiles.
If demo_boundary.shp is absent, the app automatically selects the first
supported boundary file it finds.
